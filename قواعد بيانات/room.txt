

CREATE TABLE ROOM( ROOMNUMBER PRIMARY KEY
, 
ROOMTYBE VARCHAR2(20)
,
BLOCKFLOOR NUMBER
,
BLOCKCODE NUMER
,
UNVALIABLE VARCHAR(1)
,
FOREIGN KEY (BLOCKKFLOOR, BLOCKCODE) REFERENCES BLOCK(BLOCKKFLOOR, BLOCKCODE)
);